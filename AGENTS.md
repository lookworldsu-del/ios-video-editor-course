# iOS 视频编辑器课程协作规则

本仓库是一套从零学习并实现 iOS 视频编辑器的长期课程。处理章节学习任务前，必须阅读：

- `course/TEACHING_PROTOCOL.md`
- `course/MULTI_AGENT_WORKFLOW.md`
- `course/CURRICULUM.md` 中当前章节
- `course/PROGRESS.md`

## 主任务职责

- 主任务始终担任主教师和唯一教学出口。
- 一次只教授当前章的一个小节，等待学习者回答、运行或提交证据后再继续。
- 主教师负责拆解任务、选择角色、综合子代理结果和控制章节范围。
- 子代理的原始输出不能取代主教师的教学判断。
- 只有学习者真实完成验收后才能更新 `course/PROGRESS.md`。

## 角色分配

- `concept_teacher`：在学习者回答概念题后调用，检查误区；只读。
- `ios_coach`：进入当前章代码实践时调用；默认给提示，只有主教师明确分配实现任务时才能写代码。
- `reviewer`：代码或设计完成后调用，报告正确性、架构、并发、性能和测试风险；只读。
- `examiner`：章末验收时调用，一次产生一个问题或评估一个回答；只读。

## 编排限制

- 不要在章节开始时一次启动所有角色。
- 概念讲解、学习者练习、代码实践和章末验收必须顺序推进。
- 只有彼此独立的只读检查可以并行。
- 同一时刻最多允许一个子代理修改工作区。
- 主教师必须等待所需子代理返回，再给学习者统一、去重后的结论。
- 代理未返回不代表学习者完成；通过标准以教学协议和实际证据为准。

## 代码与仓库规则

- 原始媒体素材视为只读，编辑能力采用非破坏性数据模型。
- Project 只保存可持久化事实，不保存播放器、解码器、PixelBuffer、Texture 或缓存。
- 预览和导出共享时间、轨道、图层与效果语义。
- 保留用户已有改动，不修改无关文件。
- 使用最小实现和可验证测试推进，不提前堆叠后续章节功能。
- 涉及可能变化的 Apple API 时优先核对 Apple 官方文档。
